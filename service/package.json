{
  "name": "vite-lab-service",
  "version": "1.0.0",
  "description": "",
  "main": "src/index",
  "type": "module",
  "scripts": {
    "dev": "run-p dev:*",
    "dev:build": "esbuild ./src/index.ts --watch --bundle --minify --format=esm --platform=node --target=es2020 --outfile=dist/server.js --banner:js=\"import { createRequire } from 'module';const require = createRequire(import.meta.url);\"",
    "dev:run": "nodemon -w dist dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.10.2",
    "fastify-sqlite": "^1.1.0",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/node": "^18.11.15",
    "esbuild": "^0.16.7",
    "npm-run-all": "^4.1.5",
    "turbo": "^1.6.3"
  }
}
